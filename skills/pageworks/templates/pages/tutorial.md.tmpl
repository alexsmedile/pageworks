---
title: <Tutorial title — verb-led, e.g., "Build your first microservice">
description: <One sentence — what the reader will have running at the end>
section: <section-id>
type: tutorial
status: draft
owner: <team-or-role — e.g., "@onboarding-guild">
last_reviewed: <YYYY-MM-DD>
updated: <YYYY-MM-DD>
prerequisites:
  - <Skill, tool, or knowledge required>
estimated_time: <e.g., 15 minutes>
---

# <Tutorial title>

<One-paragraph framing — what we are building together and why it matters. Avoid jargon; write to a first-time onboarder.>

## What you will have at the end

<Concrete artifact — a running local microservice, a deployed endpoint, a verified connection. Show the exact end state.>

## Before you start

<Prerequisites in plain language with exact commands to verify them.>

```bash
docker --version
node --version
```

## Step 1 — <Action-oriented first step — e.g., 'Clone and configure the repository'>

<Imperative voice. One step does one concrete thing. Show the command, then show the expected output the reader will see.>

```bash
git clone git@github.com:org/<repo>.git
cd <repo>
cp .env.example .env
```

```
✓ Cloned repository
✓ Environment file initialized
```

If you see `<COMMON_ERROR>`, `<one-line fix or link>`.

## Step 2 — <Action-oriented next step — e.g., 'Launch local dependencies'>

```bash
docker compose up -d
```

## Step 3 — <Action-oriented verification step — e.g., 'Verify the running service'>

<End every tutorial with a concrete verification command the reader executes themselves.>

```bash
curl -f http://localhost:8080/healthz
```

```json
{"status":"healthy","version":"1.0.0"}
```

## What just happened

<2–4 sentences explaining the underlying mental model. This is the only place a tutorial pauses to explain — and it is at the end, after tangible success.>

## Next

- [How-to: Deploy to Staging](../operations/deploy-staging.md)
- [Reference: Configuration Variables](../reference/config-env.md)
- [Explanation: Architecture Overview](../architecture/system-overview.md)
